CREATE TABLE usuario(
    id_usuario BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    nome VARCHAR(50) NOT NULL,
    sobrenome VARCHAR(150) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    disponibilidade VARCHAR(25) NOT NULL,
    linguagem VARCHAR(30) NOT NULL,
    area VARCHAR(50) NOT NULL,
    descricao VARCHAR(300) NOT NULL,
    data_cadastro TIMESTAMP NOT NULL,
    status VARCHAR(14) CHECK (STATUS IN ('FILA_DE_ESPERA', 'APROVADO', 'REPROVADO','CANCELADO','CONCLUIDO')) NOT NULL,

    CONSTRAINT pk_usuario PRIMARY KEY(id_usuario)
);

ALTER TABLE usuario
    ADD CONSTRAINT uc_usuario_email UNIQUE (email);